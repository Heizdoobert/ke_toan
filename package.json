{
  "name": "ultra-reconciler-pro",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "echo 'No compilation build step required for modern vanilla JS.'",
    "start": "node server.js",
    "lint": "echo 'Modern vanilla JS requires no custom lint rules.'",
    "clean": "rm -rf server/recon_db.json"
  },
  "dependencies": {
    "express": "^4.21.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^22.14.0"
  }
}
